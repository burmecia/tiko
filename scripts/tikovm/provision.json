{
    "vm_id": "vm-1",
    "rootfs": {
        "path": "$ASSETS/echo-rootfs.ext4",
        "read_only_base": true
    },
    "resources": {
        "memory_mb": 1024,
        "vcpus": 2
    },
    "kernel": {
        "kernel_path": "$ASSETS/vmlinux-6.1",
        "kernel_cmdline": "console=ttyS0 reboot=k panic=1 pci=off systemd.unified_cgroup_hierarchy=0",
        "initrd_path": "$ASSETS/tiko-initramfs.cpio.gz"
    },
    "network": {},
    "manifest": {
        "version": 1,
        "workload": "echo",
        "health": {
            "kind": "none"
        }
    }
}
